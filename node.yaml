name: yaml
npm::name: "@fnet/yaml"

s::repo.url: git+https://gitlab.com/fnetai/yaml.git

# s::features.cli.extend: true
s::features.rollup.iife: false
s::features.app: false

inputs:
  - name: content
    type: string
    description: |
      The YAML content to parse.
    alias: c
    
  - name: file
    type: string
    description: |
      The YAML file to parse.
    alias: f
