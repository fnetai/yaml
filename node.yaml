name: yaml

ai::name: "@fnet/yaml"

s::repo.url: https://gitlab.com/fnetai/yaml.git

input: g::file://fnet/input.yaml

features:
  dts: true
  app: false
  cli: false

  t::dev::cli: true
  t::py::s::runtime.type: "python"
